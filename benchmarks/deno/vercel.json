{
  "buildCommand": "echo 'No build required'",
  "devCommand": "deno run --allow-net --allow-read --allow-env hono.ts",
  "installCommand": "npm install",
  "framework": null,
  "outputDirectory": ".",
  "env": {
    "DENO_DIR": ".deno"
  }
}
